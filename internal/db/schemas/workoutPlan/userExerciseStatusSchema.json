{
  "$jsonSchema": {
    "bsonType": "object",
    "required": [
      "userId",
      "exerciseId",
      "workoutPlanId",
      "completed",
      "completedLogs"
    ],
    "properties": {
      "userId": {
        "bsonType": "objectId",
        "description": "must be an objectId and is required"
      },
      "exerciseId": {
        "bsonType": "objectId",
        "description": "must be an objectId and is required"
      },
      "circuitId": {
        "bsonType": ["objectId", "null"],
        "description": "can be an objectId or null and is optional"
      },
      "workoutItemId": {
        "bsonType": ["objectId", "null"],
        "description": "can be an objectId or null and is optional"
      },
      "workoutPlanId": {
        "bsonType": "objectId",
        "description": "must be an objectId and is required"
      },
      "completed": {
        "bsonType": "bool",
        "description": "must be a boolean and is required"
      },
      "completedLogs": {
        "bsonType": "array",
        "description": "must be an array of ExerciseLog objects and is required",
        "items": {
          "bsonType": "object",
          "properties": {
            "setNumber": {
              "bsonType": ["int", "null"],
              "description": "can be null or an integer"
            },
            "reps": {
              "bsonType": ["int", "null"],
              "description": "can be null or an integer"
            },
            "weight": {
              "bsonType": ["double", "null"],
              "description": "can be null or a double"
            }
          }
        }
      }
    }
  }
}
