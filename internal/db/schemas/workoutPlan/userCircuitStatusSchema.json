{
  "$jsonSchema": {
    "bsonType": "object",
    "required": ["userId", "circuitId", "workoutDayId", "workoutPlanId"],
    "properties": {
      "_id": {
        "bsonType": "objectId"
      },
      "userId": {
        "bsonType": "objectId",
        "description": "User ID for whom the circuit completion status is tracked."
      },
      "circuitId": {
        "bsonType": "objectId",
        "description": "Circuit ID that the status is tracked for."
      },
      "workoutDayId": {
        "bsonType": "objectId",
        "description": "Workout day ID that the status is tracked for."
      },
      "workoutPlanId": {
        "bsonType": "objectId",
        "description": "Workout plan ID that the status is tracked for."
      },
      "completed": {
        "bsonType": "bool",
        "description": "Indicates whether the circuit has been completed by the user."
      }
    }
  }
}
